version: 2
updates:
  # Maintain dependencies for GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    
  # (Optional) If we add a package.json later for Node.js
  - package-ecosystem: "npm"
    directory: "/src"
    schedule:
      interval: "daily"